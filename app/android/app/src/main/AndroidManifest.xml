<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Talkie-walkie des parties multijoueur : se parler pendant un jeu.
         Demandée à la volée, au premier appui sur le bouton « parler ». -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Égaliseur : Android exige cette permission pour créer un effet audio
         sur la session du lecteur. -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <!-- Auto-update : lancement de l'installeur d'APK téléchargé -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <!-- Lecture continue écran éteint : sans cette exemption, Android coupe
         le réseau de l'app en sommeil profond et le flux s'arrête. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- Réveil matinal (idée #81). Une alarme « à l'heure dite » est la seule
         qu'Android ne décale pas en sommeil profond :
           · USE_EXACT_ALARM (Android 13+) est accordée à l'installation ;
           · SCHEDULE_EXACT_ALARM couvre Android 12, où elle se règle à la main
             (l'écran du réveil le dit et propose d'ouvrir le réglage).
         RECEIVE_BOOT_COMPLETED repose l'alarme après un redémarrage — sinon
         Android l'oublie et le réveil ne sonne jamais. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Ouverture plein écran du réveil par-dessus l'écran verrouillé. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- Google TV. Un seul APK sert le téléphone et le téléviseur : c'est au
         démarrage que l'app demande à Android sur quoi elle s'ouvre, et bascule
         vers l'interface à dix pieds.

         · touchscreen non requise, sinon le Play Store considère l'app comme
           incompatible avec les téléviseurs et le lanceur TV ne la voit pas ;
         · leanback souhaitée mais non requise, sinon on perdrait les
           téléphones ;
         · le micro et la télécommande d'un boîtier ne sont pas garantis. -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false"/>
    <uses-feature android:name="android.software.leanback" android:required="false"/>
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>

    <application
        android:label="Gullify"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:banner="@drawable/tv_banner"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <!-- La même activité sert de point d'entrée au lanceur Google
                     TV : c'est cette catégorie, et elle seule, qui fait
                     apparaître Gullify sur la rangée d'applications du
                     téléviseur. -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- audio_service playback service; MediaBrowserService intent-filter
             is what makes the app appear in Android Auto. -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService"/>
            </intent-filter>
        </service>

        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON"/>
            </intent-filter>
        </receiver>

        <!-- Auto-update : expose l'APK téléchargé (cache) à l'installeur -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

        <!-- Réveil matinal : l'alarme, son filet audible, et la repose de
             l'alarme après un redémarrage du téléphone (idée #81). -->
        <receiver
            android:name=".AlarmReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="app.gullify.gullify.ALARM_RING"/>
                <action android:name="app.gullify.gullify.ALARM_SAFETY"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>

        <!-- Widget d'écran d'accueil -->
        <receiver
            android:name=".GullifyWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/gullify_widget_info"/>
        </receiver>

        <!-- Android Auto declaration -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc"/>

        <!-- Impeller cause des artefacts BackdropFilter (verre rendu plein
             écran) sur certains GPU — Skia est fiable pour notre usage. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
